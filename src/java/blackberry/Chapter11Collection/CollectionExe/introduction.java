package blackberry.Chapter11Collection.CollectionExe;

public class introduction {
    /**
     *  Collection练习题目
     *  1、定义一个Employee类。
     *      改类包含：private成员变量，name,age,birthday，其中国birthday为MyDate类的对象
     *      并未每一个属性定义getter,setter方法，并重写toString方法输出name,age,birthday
     *
     *
     *      MyDate类包含：
     *      private成员变量year,month,day,并为每一个属性定义getter,setter方法
     *
     *
     *      创建该类的5个对象，并把这些对象放入TreeSet集合中（下一章：TreeSet需使用泛型来定义）
     *
     *      分别按照一下两种方式对集合中的元素进行排序，并遍历输出
     *
     *      1）使用Employee实现Comparable接口，并按name排序
     *      2）创建TreeSet时传入Comparator对象，按生日日期的先后排序
    */


}
